{
  "name": "hms-workspace",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -k -n backend,frontend -c blue,green \"cd backend && PYTHONPATH=. ./.venv/bin/python -m uvicorn app.main:app --reload\" \"cd frontend && npm start\"",
    "dev:backend": "cd backend && PYTHONPATH=. ./.venv/bin/python -m uvicorn app.main:app --reload",
    "dev:frontend": "cd frontend && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
